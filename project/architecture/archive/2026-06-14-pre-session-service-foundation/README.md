# Pre-session-service foundation architecture archive

This folder preserves the architecture documents immediately before the v0.4 `session-application-foundation` feature changed runtime ownership from WebSocket connection state to `SessionService`.

Current architecture source remains:

- `project/architecture/core-session-engine.md`
- `project/architecture/researcher-console.md`
